@AGENTS.md

Think before coding. Keep diffs surgical. Red-green TDD is law for new work:
write a failing test first, then the minimum code to pass, then refactor.
This repository is the Hawkeye binary only — do not vendor hawkeye-data.
Never add a public chat UI. Apply defaults to dry-run. Redact secrets before
any LLM or MCP payload. Git author is Mark LaPointe <mark@cloudbsd.org>.
